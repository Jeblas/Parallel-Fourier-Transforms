#include "Multi_Threaded/threads_dtf.cc"
#include "MPI/mpi_fft.cc"
#include "Cuda/cuda_naive.cu"

int main(int argc, char **argv) {
    // check for correct number of arguments

    //define tags for the variants
    // main_multi(argc, argv);
    // main_mpi(argc, argv);
    // main_cuda(argc, argv);

    
    // check for reverse argument
    

    return 0;
}